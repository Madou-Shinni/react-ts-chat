import React from "react";
import SeperatorWrapper from "./style";

type Props = {
}

const Seperator: React.FC<Props> = (props) => {
    return <SeperatorWrapper {...props}>
    </SeperatorWrapper>
}


export default Seperator;
