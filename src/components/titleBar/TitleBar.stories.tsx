import React from "react";
import TitleBar from "./TitleBar";

import '../../story.css'

export default {
    title: "UI/TitleBar",
    component: TitleBar
}

export const Default = () => {
    return <TitleBar />
}
