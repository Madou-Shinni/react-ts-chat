import React from "react";
import FileListWrapper from "./style";

type Props = {
}

const FileList: React.FC<Props> = (props) => {
    return <FileListWrapper {...props}>
    </FileListWrapper>
}


export default FileList;
