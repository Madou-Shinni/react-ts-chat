import styled from "styled-components"

interface StyleProps {
}

const FilterListWrapper = styled.div<StyleProps>`
  padding: 30px;
  height: 100vh;
  overflow-y: auto;
`


export default FilterListWrapper;
